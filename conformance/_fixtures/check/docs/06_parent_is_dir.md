---
artifact_type: plan
authority: derived
generated_by: conformance-fixture
parent_artifacts:
  - .
---
# 06 — Parent path is a directory

The parent `.` resolves to the repo root directory. Engine must NOT crash (G6).
