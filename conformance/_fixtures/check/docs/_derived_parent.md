---
artifact_type: plan
authority: derived
generated_by: conformance-fixture
parent_artifacts: []
---
# Derived parent

A `plan` doc with derived authority. Used as the parent by 20 to exercise the
`structured ← derived` case (which the narrowed inversion gate must NOT warn on).
