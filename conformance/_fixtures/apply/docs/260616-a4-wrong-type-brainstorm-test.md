---
artifact_type: findings
authority: structured
generated_by: manual
parent_artifacts: []
---
# A4 — Wrong Type

Filename says `brainstorm` but frontmatter says `findings`. _apply should correct type to `brainstorm` and authority to `speculative`.
